% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_variance_from_pdf}
\alias{get_variance_from_pdf}
\title{Calculate Variance from Density Function (Juanchi)}
\usage{
get_variance_from_pdf(density_fn, lower = -Inf, upper = Inf)
}
\arguments{
\item{density_fn}{The probability density function f(x).}

\item{lower}{Lower bound of the domain (default -Inf).}

\item{upper}{Upper bound of the domain (default Inf).}
}
\value{
A numeric value representing the variance. To get Standard Deviation, apply sqrt() to the result.
}
\description{
Calculates the Variance of a continuous random variable given its density function.
It automatically calculates the Mean first to use the variance formula.

Formula: E(X^2) - (E(X)^2
}
\examples{
# Example: Uniform distribution between 0 and 10 -> f(x) = 1/10
f_unif <- function(x) ifelse(x >= 0 & x <= 10, 1/10, 0)

# Calculate Variance
var_val <- get_variance_from_pdf(f_unif, lower = 0, upper = 10)
print(var_val)

# Calculate Standard Deviation
sqrt(var_val)
}
