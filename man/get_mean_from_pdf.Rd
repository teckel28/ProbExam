% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_mean_from_pdf}
\alias{get_mean_from_pdf}
\title{Calculate Mean (Expected Value) from Density Function (Juanchi)}
\usage{
get_mean_from_pdf(density_fn, lower = -Inf, upper = Inf)
}
\arguments{
\item{density_fn}{The probability density function f(x).}

\item{lower}{Lower bound of the domain (default -Inf).}

\item{upper}{Upper bound of the domain (default Inf).}
}
\value{
A numeric value representing the mean.
}
\description{
Calculates the center of gravity (Expected Value) of a continuous random variable
given its density function f(x).

Formula: Integral of x * f(x)
}
\examples{
# Example: f(x) = 0.2x for 0-2, and 0.4x-0.4 for 2-3
f_piecewise <- function(x) {
  if (x >= 0 && x <= 2) return(0.2 * x)
  if (x > 2 && x <= 3) return(0.4 * x - 0.4)
  return(0)
}

# Calculate Mean
get_mean_from_pdf(f_piecewise, lower = 0, upper = 3)
}
